import { existsSync, readFileSync } from "node:fs";

const index = readFileSync(new URL("../index.html", import.meta.url), "utf8");
const script = readFileSync(new URL("../script.js", import.meta.url), "utf8");
const styles = readFileSync(new URL("../styles.css", import.meta.url), "utf8");
const faviconExists = existsSync(new URL("../favicon.svg", import.meta.url));
const favicon = faviconExists ? readFileSync(new URL("../favicon.svg", import.meta.url), "utf8") : "";

const checks = [
  ["page declares a favicon so browser preview does not 404", /<link\s+rel="icon"\s+href="favicon\.svg"\s+type="image\/svg\+xml"\s*\/?>/],
  ["favicon asset exists", faviconExists ? /./ : /$a/],
  ["city tape logo markup is used across splash auth and header", /splash-logo[\s\S]*logo-card-mark[\s\S]*auth-brand[\s\S]*logo-card-mark[\s\S]*wordmark[\s\S]*logo-card-mark/],
  ["city tape logo styles define card bands", /\.logo-card-mark[\s\S]*\.logo-band\.is-blue[\s\S]*\.logo-band\.is-orange/],
  ["favicon uses the city tape logo direction", /city-tape-favicon/],
  ["mobile viewport disables user scaling for app-like demo", /name="viewport"\s+content="[^"]*maximum-scale=1\.0[^"]*user-scalable=no/],
  ["mobile viewport allows safe-area fitting for iPhone app shell", /name="viewport"\s+content="[^"]*viewport-fit=cover/],
  ["app shell disables text selection and touch callout", /user-select:\s*none[\s\S]*-webkit-user-select:\s*none[\s\S]*-webkit-touch-callout:\s*none/],
  ["app shell disables copy paste selection browser gestures", /installAppInteractionGuards[\s\S]*\["copy",\s*"cut",\s*"paste",\s*"selectstart",\s*"contextmenu",\s*"dragstart"\][\s\S]*gesturestart[\s\S]*dblclick/],
  ["prototype storage resets for rich demo data", /LOOP_DATA_VERSION[\s\S]*20260618-rich-demo-v1/],
  ["demo account is seeded with rich records and pass orders", /function demoUserSnapshot[\s\S]*records:\s*defaultRecords\(\)[\s\S]*featuredPasses:\s*demoFeaturedPasses\(\)[\s\S]*completedRouteIds:\s*demoCompletedRouteIds\(\)/],
  ["demo data includes active completed and expired city pass states", /demoFeaturedPasses[\s\S]*"active"[\s\S]*"completed"[\s\S]*"expired"/],
  ["default simulated records include at least forty demo entries", /function defaultRecords\(\)[\s\S]*金桥周末宠物友好线/],
  ["city pass category is defined before 秘境探索", /id:\s*"featured"[\s\S]*?name:\s*"城市通行证"[\s\S]*?id:\s*"quest"[\s\S]*?name:\s*"秘境探索"/],
  ["city pass category has distinct styling hooks", /featuredLayerVisual|is-featured-layer|--featured-accent/],
  ["featured map pass data includes multi-city issues", /featuredPassMaps[\s\S]*?shanghai[\s\S]*?chengdu[\s\S]*?abudhabi/],
  ["route detail can buy a city pass", /buyFeaturedPass|购买通行证|开启这张地图/],
  ["route detail can redeem pass stops", /redeemFeaturedStop|到店核销|已核销|未使用|已过期/],
  ["profile renders ongoing city passes", /renderFeaturedPasses|进行中的城市通行证|继续查看地图/],
  ["featured pass has only paid lifecycle states", /active|completed|expired|已支付|使用中|已完成/],
  ["featured pass has simulated payment without saved pending order", /payFeaturedPass|模拟支付|passPaymentPreviewHTML/],
  ["redemption requires QR or redemption code", /openRedemptionCode|qr-code|核销码|模拟店员扫码核销/],
  ["redemption opens as a modal instead of inline route content", /passActionSheet|pass-action-sheet|renderPassActionSheet[\s\S]*redemptionCodePanelHTML/],
  ["redemption code is not injected into the route progress area", { not: /routeProgress\.innerHTML[\s\S]*redemptionPanel/ }],
  ["featured pass state persists to the signed in user", /hydrateUserState|persistUserState|serializeExplorationState|featuredPasses:\s*state\.featuredPasses|records:\s*state\.records/],
  ["purchased featured pass reopens as exploration instead of purchase", /openRouteDetail[\s\S]*ownedFeaturedPass[\s\S]*routeDetailMode\s*=\s*ownedFeaturedPass[\s\S]*active/],
  ["starting another route requires confirmation and archives current progress", /confirmExplorationSwitch|archiveInterruptedExploration|切换地图|阶段保存/],
  ["route switch confirmation is in-app instead of native browser alert", /switchConfirmSheet|openExplorationSwitchConfirm|switchConfirmButton|继续当前地图|保存并切换/],
  ["route switch flow does not call native window confirm", { not: /window\.confirm/ }],
  ["payment waits for switch confirmation before creating a new pass", /payFeaturedPass\(routeItem,\s*options\s*=\s*\{\}\)[\s\S]*confirmExplorationSwitch\(routeItem,\s*"pay-featured"\)/],
  ["completed featured pass exits active exploration", /completeFeaturedPass[\s\S]*clearExplorationStateOnly/],
  ["profile has order and settings entry points", /ordersButton|settingsButton|订单|设置/],
  ["demo credentials can be typed manually as well as using the demo button", /DEMO_ACCOUNT[\s\S]*DEMO_PASSWORD[\s\S]*function loginWithCredentials\(accountValue,\s*passwordValue[\s\S]*account === DEMO_ACCOUNT[\s\S]*passwordValue === DEMO_PASSWORD[\s\S]*ensureDemoUser/],
  ["completed city passes are not shown as a separate profile section", { not: /已完成城市通行证|completed-pass-section|completedPassSection|renderCompletedFeaturedPasses/ }],
  ["completed city passes are saved into today exploration records", /completeFeaturedPass[\s\S]*state\.records\.push[\s\S]*城市通行证完成/],
  ["completed city pass records share the normal record card structure with subtle color hooks", /recordCard[\s\S]*isFeaturedPassRecord[\s\S]*record-card\$\{isFeaturedPassRecord[\s\S]*record-status-pill[\s\S]*通行证完成/],
  ["ordinary exploration records do not get an extra top status pill", { not: /record-status-pill is-quiet/ }],
  ["city pass record color treatment does not replace the normal card background", { not: /\.record-card\.is-featured-pass-record\s*\{[^}]*background:/ }],
  ["city pass records are sorted before ordinary exploration records", /recordPriority[\s\S]*城市通行证完成[\s\S]*allRecords[\s\S]*recordPriority\(b\) - recordPriority\(a\)/],
  ["purchased pass card has a visible paid state", /已购买|已支付|pass-owned|继续下一站/],
  ["featured pass price uses editorial metadata treatment", /pass-price-main|通行证价|pass-price-meta/],
  ["route and pass details use horizontal stop cards", /route-stop-carousel|route-stop-card|pass-stop-carousel/],
  ["route details show a subway style stop line above cards", /route-line-map|route-line-stop|bindRouteCarouselControls[\s\S]*syncRouteLineFocus/],
  ["city pass route line uses store names instead of numbered stop labels", /passStopCarouselHTML[\s\S]*routeItem\.benefits\.map\(\(item\) => item\.store\)/],
  ["route line focus updates while users swipe cards", /nearestRouteCardIndex|requestAnimationFrame[\s\S]*syncRouteLineFocus/],
  ["route carousel uses stable scroll math instead of scrollIntoView jumps", /scrollCarouselToIndex[\s\S]*carousel\.scrollTo[\s\S]*routeScrollLock/],
  ["route carousel no longer calls scrollIntoView for stop cards", { not: /route-stop-card[\s\S]{0,500}scrollIntoView|focused\.scrollIntoView|target\.scrollIntoView/ }],
  ["route stop cards include images", /route-card-photo|stopPhotoForRoute|stopPhotoForPass/],
  ["city pass cards show both store environment and benefit product images", /pass-card-media[\s\S]*pass-store-photo[\s\S]*pass-benefit-photo|benefitPhotoForPass/],
  ["city pass cards use a dedicated non-overlapping information layout", /pass-card-main[\s\S]*pass-card-topline[\s\S]*pass-card-titleline[\s\S]*pass-benefit-inline/],
  ["city pass card main does not inherit route card grid placement", /pass-card-main[\s\S]*display:\s*flex[\s\S]*flex-direction:\s*column/],
  ["city pass card separates redemption action from benefit copy and media", /pass-station-action[\s\S]*pass-card-titleline[\s\S]*pass-benefit-inline/],
  ["city pass station cards include production-like location status details", /pass-live-details[\s\S]*营业|预计步行|核销窗口/],
  ["city pass carousel shows one complete station card at a time", /route-stop-stage\.pass-stop-carousel\s+\.route-stop-card[\s\S]*flex:\s*0\s+0\s+100%[\s\S]*width:\s*100%/],
  ["city pass cards override route stop span numbering rules", /route-stop-card\.pass-stop-card span[\s\S]*grid-row:\s*auto[\s\S]*width:\s*auto[\s\S]*height:\s*auto/],
  ["city pass action pill beats generic pass span reset", /route-stop-card\.pass-stop-card span\.pass-station-action[\s\S]*display:\s*inline-grid[\s\S]*border-radius:\s*999px[\s\S]*background:\s*rgba\(32,\s*36,\s*43,\s*0\.07\)/],
  ["legacy virtual-map rules do not target every nested card element", { not: /\.virtual-map\s+(div|span|strong|button)\s*\{/ }],
  ["payment preview uses dedicated copy and media classes", /pass-payment-hero-media|pass-payment-hero-copy|pass-payment-summary/],
  ["route stop carousel has explicit left right controls", /data-route-scroll|route-scroll-button|bindRouteCarouselControls/],
  ["route stop carousel supports touch swiping", /touch-action:\s*pan-x|-webkit-overflow-scrolling:\s*touch/],
  ["horizontal stop area has explicit visible height", /route-stop-carousel[\s\S]*height:\s*clamp/],
  ["horizontal stop carousel resets old virtual-map grid columns", /\.virtual-map\s+\.route-stop-carousel[\s\S]*grid-template-columns:\s*none/],
  ["route stop card body is not visually clipped", { not: /\.route-stop-card p\s*\{[^}]*-webkit-line-clamp/ }],
  ["route detail does not force a blank 1fr map row", { not: /grid-template-rows:\s*auto\s+minmax\(132px,\s*1fr\)/ }],
  ["route detail image strip is removed from compact detail sheet", /\.route-detail-sheet\s+\.route-detail-photo[\s\S]*display:\s*none/],
  ["route detail hidden attribute is visually respected", /\.route-detail-sheet\[hidden\][\s\S]*display:\s*none\s*!important/],
  ["payment preview is temporary and concise", /pass-payment-preview|pass-payment-hero|订单摘要|确认后开启/],
  ["payment preview does not reuse the swipe carousel", /function renderFeaturedPaymentPreview\(routeItem\) \{[\s\S]{0,500}passPaymentPreviewCardHTML\(routeItem\)[\s\S]{0,500}passPaymentPreviewHTML\(routeItem\);[\s\S]{0,120}\}/],
  ["featured pass hero images use pass store photos instead of fallback source", /function photoForRoute\(routeItem[\s\S]{0,260}isFeaturedPass[\s\S]{0,260}stopPhotoForPass/],
  ["paid pass enters normal exploration flow with redemption", /startFeaturedPassExploration|openRedemptionCode|核销码|定位打卡/],
  ["paid pass detail does not render redundant exploration summary block", { not: /passExplorationPanelHTML|pass-exploration-panel/ }],
  ["checkin and photo actions open sub sheets before changing state", /openCheckinSheet|openPhotoSheet|passActionSheet/],
  ["checkin button no longer advances the station immediately", /dom\.checkinButton\.addEventListener\("click",\s*\(\)\s*=>\s*openCheckinSheet/],
  ["photo flow asks camera or upload before saving", /data-photo-source="camera"[\s\S]*data-photo-source="upload"|camera-frame|upload-drop/],
  ["photo save writes a record into today week month year history", /savePhotoRecord[\s\S]*state\.records\.(unshift|push)[\s\S]*photoSource/],
  ["profile duplicated hero ticket is removed", { not: /folioCover\.innerHTML[\s\S]*ticket-top/ }],
  ["profile record section no longer has meaningless add photo button", { not: /fakePhotoButton|加照片/ }],
  ["profile top record period bar is removed", { not: new RegExp(`record${"Tabs"}|record${"-tabs"}|记录${"周期"}`) }],
  ["profile keeps the lower period overview selector", /periodOverview|period-overview|renderPeriodOverview/],
  ["profile record summary is wrapped as a section-local sticky header", /profile-record-section[\s\S]*profile-record-sticky-head[\s\S]*recordListTitle[\s\S]*periodOverview/],
  ["profile record sticky header is constrained by its record section", /\.profile-record-section[\s\S]*position:\s*relative[\s\S]*\.profile-record-sticky-head[\s\S]*position:\s*sticky[\s\S]*top:\s*calc\(var\(--header-safe-top\)\s*\+\s*32px\)[\s\S]*z-index:\s*22/],
  ["profile record detail opens a single exploration record", /openRecordDetail\(record\.id\)[\s\S]*function openRecordDetail\(recordId\)[\s\S]*renderRecordDetailSheet\(record,\s*\[record\]/],
  ["single record detail does not render the full day list", /function openRecordDetail\(recordId\)[\s\S]{0,500}renderRecordDetailSheet\(record,\s*\[record\]/],
  ["profile record list initially shows at most six records", /PROFILE_RECORD_PAGE_SIZE\s*=\s*6[\s\S]*visibleProfileRecords/],
  ["profile record list can load six more or expand all", /data-record-list-action="more"[\s\S]*继续展开[\s\S]*data-record-list-action="all"[\s\S]*展开全部/],
  ["profile record controls can be dismissed with a close button", /profileRecordControlsDismissed[\s\S]*data-record-list-action="dismiss"[\s\S]*aria-label="关闭记录展开选项"/],
  ["profile inline record controls do not show bottom jump", { not: /recordListControls[\s\S]*一键到底/ }],
  ["profile expanded records can jump top and collapse inline", /data-record-list-action="top"[\s\S]*一键到顶[\s\S]*data-record-list-action="collapse"[\s\S]*恢复折叠/],
  ["profile scroll shortcut is a single mobile top pill", /record-scroll-float[\s\S]*data-record-float-action="top"[\s\S]*回到顶部[\s\S]*handleProfileScrollFloat/],
  ["profile scroll shortcut has no web-style bottom jump", { not: /data-record-float-action="bottom"|跳到记录底部|>↓</ }],
  ["profile floating scroll pill is styled and wired", /(?=[\s\S]*\.record-scroll-float[\s\S]*position:\s*fixed)(?=[\s\S]*\.record-scroll-float button[\s\S]*border-radius:\s*999px)(?=[\s\S]*dom\.appFrame\.addEventListener\("scroll",\s*syncRecordScrollFloat\))(?=[\s\S]*dom\.recordScrollFloat\.addEventListener\("click",\s*handleProfileScrollFloat\))/],
  ["profile floating scroll pill fades after scrolling stops", /recordScrollFloatTimer[\s\S]*setTimeout[\s\S]*dom\.recordScrollFloat\.hidden\s*=\s*true/],
  ["profile record expansion resets when period changes", /resetProfileRecordFold[\s\S]*recordViewFromLabel/],
  ["profile record detail renders a photo carousel from station photos", /recordPhotos[\s\S]*detail-photo-track[\s\S]*renderRecordDetailSheet[\s\S]*recordPhotos\(focused/],
  ["each station allows only one saved photo", /savePhotoRecord[\s\S]*existingPhoto[\s\S]*return false[\s\S]*stopIndex/],
  ["profile record map has all stations view with photo and checkin states", /所有站点[\s\S]*recordViewFromLabel[\s\S]*places[\s\S]*stationGridCells[\s\S]*has-checkin-no-photo/],
  ["active ordinary route cards show continue next station in map lists", /(?=[\s\S]*function isOrdinaryRouteActive\(routeItem\)[\s\S]*state\.explorationActive[\s\S]*state\.explorationRoute\?\.id === routeItem\.id)(?=[\s\S]*function ordinaryRouteCardActionText\(routeItem\)[\s\S]*继续下一站)(?=[\s\S]*routeCard[\s\S]*ordinaryRouteCardActionText\(routeItem\))/],
  ["profile renders ongoing ordinary interest maps separately", /interestMapSection[\s\S]*进行中的兴趣地图[\s\S]*renderOngoingInterestMap/],
  ["demo profile seeds enough ongoing city pass cards to exercise horizontal scrolling", /(?=[\s\S]*DEMO_PREVIEW_PASS_TARGET\s*=\s*3)(?=[\s\S]*function demoPreviewFeaturedPassItems)(?=[\s\S]*renderOngoingFeaturedPasses\(demoPreviewFeaturedPassItems\(activeItems\)\))/],
  ["demo profile seeds enough ongoing interest map cards to exercise horizontal scrolling", /(?=[\s\S]*DEMO_PREVIEW_INTEREST_MAP_TARGET\s*=\s*3)(?=[\s\S]*function demoPreviewInterestMapItems)(?=[\s\S]*routeItems\s*=\s*demoPreviewInterestMapItems\(routeItem\))/],
  ["ongoing city pass cards show pass price in the top-right status row", /profile-pass-topline[\s\S]*profile-pass-price[\s\S]*\$\{route\.price \|\| route\.budget\}/],
  ["ongoing city pass price aligns with the left status text", /\.profile-pass-topline[\s\S]*display:\s*flex[\s\S]*align-items:\s*baseline[\s\S]*justify-content:\s*space-between[\s\S]*\.profile-pass-price/],
  ["profile ongoing interest maps use the same horizontal rail as city passes", /interestMapList\.className\s*=\s*`profile-pass-list interest-map-list is-compact has-tilted-covers/],
  ["profile tilted ongoing rails allow horizontal touch panning", /\.profile-pass-list\.is-compact\.has-tilted-covers[\s\S]*touch-action:\s*pan-x/],
  ["profile floating top shortcut avoids ongoing exploration bar", /\.app-frame\.has-ongoing\s+\.record-scroll-float[\s\S]*bottom:\s*calc/],
  ["home refresh control uses a compact editorial tag", /save-bubble-mark[\s\S]*换灵感[\s\S]*\.save-bubble[\s\S]*border-radius:\s*12px[\s\S]*rgba\(32,\s*36,\s*43,\s*0\.30\)[\s\S]*\.save-bubble-mark[\s\S]*var\(--orange\)/],
  ["home reveal cue uses a quieter pull handle", /swipe-cue-handle/],
  ["home reveal cue teaches downward map generation", /向下滑动生成地图[\s\S]*LOOP 会把灵感变成路线/],
  ["home reveal cue treats current inspiration as the default selection", /当前灵感会自动生成路线[\s\S]*dom\.swipeUpCue\.hidden\s*=\s*state\.routesRevealed\s*\|\|\s*state\.revealAnimating[\s\S]*dom\.swipeUpCue\.setAttribute\("aria-disabled",\s*"false"\)[\s\S]*startRouteReveal[\s\S]*state\.inspirationSelected\s*=\s*true/],
  ["home pull cue no longer asks users to pick before pulling", { not: /选一个灵感，下拉生成地图|先选一个今天靠近的灵感/ }],
  ["home inspiration carousel aligns with lower content edge", /mood-panel\s+\.mood-grid[\s\S]*margin:\s*0[\s\S]*padding:\s*8px\s+0\s+8px[\s\S]*scroll-padding-inline:\s*0/],
  ["home inspiration carousel uses stable scroll math instead of scrollIntoView", /syncMoodRailFocus[\s\S]*getBoundingClientRect[\s\S]*scrollTo[\s\S]*clampNumber/],
  ["home inspiration carousel does not center selected cards with scrollIntoView", { not: /button\.scrollIntoView\(\{\s*behavior:\s*"smooth",\s*block:\s*"nearest",\s*inline:\s*"center"\s*\}\)/ }],
  ["home inspiration carousel ranking is stable while selecting cards", /moodRankSeed[\s\S]*state\.feedVersion[\s\S]*moodScore[\s\S]*moodRankSeed\(context\)/],
  ["home inspiration carousel ranking is not reseeded by selected mood", { not: /moodScore[\s\S]{0,360}context\.seed/ }],
  ["home inspiration carousel pauses auto rotation after rail touch", /lastMoodRailInteractionAt[\s\S]*dom\.moodGrid\.addEventListener\("pointerdown"[\s\S]*Date\.now\(\) - state\.lastMoodRailInteractionAt < 9000/],
  ["home inspiration carousel auto rotation is not overly aggressive", /setInterval\(\(\) => \{[\s\S]*\},\s*12000\)/],
  ["home route reveal uses generated map animation instead of envelope", /route-reveal-map[\s\S]*route-map-sheet[\s\S]*city-trace[\s\S]*route-node[\s\S]*route-pulse[\s\S]*正在生成今日私人路线[\s\S]*匹配灵感、街区和步行顺序/],
  ["home route reveal private route copy is visible inside the map animation", /route-map-caption[\s\S]*正在生成今日私人路线[\s\S]*\.route-map-caption/],
  ["home route reveal uses app-like staged pull animation", /routeRevealSheet[\s\S]*routeMapFloat[\s\S]*routeTraceDraw[\s\S]*routeNodePop[\s\S]*routeCopyRise/],
  ["home route reveal uses a compact map surface", /route-reveal-map\s*\{[\s\S]*height:\s*86px/],
  ["home route reveal animations are brief", /routeRevealSheet\s+520ms[\s\S]*routeMapFloat\s+620ms[\s\S]*routeTraceDraw\s+560ms/],
  ["home route reveal stays clear of the bottom nav", /keepRouteRevealInView[\s\S]*bottomSafe[\s\S]*scrollBy/],
  ["home route reveal remains visible through the staged animation", /revealTimer\s*=\s*setTimeout\([\s\S]*\},\s*1050\)/],
  ["home route reveal avoids the old scan-strip loader", { not: /route-scan|routeScan/ }],
  ["old envelope route reveal markup is removed", { not: /envelope-back|envelope-fold|envelope-flap|tear-line|pull-tab/ }],
  ["local prototype data uses a reset version", /LOOP_DATA_VERSION[\s\S]*20260618-rich-demo-v1[\s\S]*resetPrototypeStorageIfNeeded/],
  ["default simulated records are varied and not duplicated filler", /defaultRecords\(\)[\s\S]*上海低光夜游地图[\s\S]*凌晨便利店霓虹短线[\s\S]*阿布扎比海风白墙记录/],
  ["profile ongoing city passes are compact and horizontally paged", /profile-pass-list[\s\S]*grid-auto-flow:\s*column[\s\S]*grid-auto-columns|profile-pass-list\.is-compact/],
  ["profile city pass more opens categorized pass library", /passMapButton[\s\S]*看更多|openAccountSheet\("passes"\)|renderPassLibraryPanel|pass-library-tabs/],
  ["orders use filter tabs and not exploration actions", /\["all",\s*"全部"\][\s\S]*\["active",\s*"使用中"\][\s\S]*\["completed",\s*"已完成"\][\s\S]*\["expired",\s*"已过期"\][\s\S]*order-filter-tabs/],
  ["orders do not render continue exploration buttons", { not: /data-account-pass[\s\S]*featuredPassCardActionText/ }],
  ["ordinary route detail removes duplicate overview panels", { not: /routeProgress\.innerHTML\s*=\s*routeOverviewPanelHTML|routeProgress\.innerHTML\s*=\s*activeRoutePanelHTML/ }],
  ["ordinary route primary action continues until final stop", /ordinaryRouteActionText[\s\S]*继续下一站[\s\S]*完成探索/],
  ["ordinary route cannot complete before the last stop", /completeRoute[\s\S]*isRouteAtLastStop[\s\S]*openCheckinSheet/],
  ["active city pass primary action uses next-station naming", /featuredPassDetailActionText[\s\S]*继续下一站[\s\S]*完成探索/],
  ["station next copy does not use complete exploration as a stop name", { not: /next:\s*stops\[index \+ 1\]\s*\|\|\s*"完成探索"/ }],
  ["final station checkin copy is not rendered as a next station", /finalActionHint[\s\S]*stop\.isLast[\s\S]*最后一站[\s\S]*checkinActionPanelHTML[\s\S]*finalActionHint\(stop\)/],
  ["focused non-current station returns to current station instead of showing completion", /featuredPassDetailActionText[\s\S]*routeFocusedIndex[\s\S]*回到当前站/],
  ["swiping station cards refreshes the bottom primary action", /syncRouteLineFocus[\s\S]*updateRouteActionButtons/],
  ["active city pass final route action does not claim the whole pass is complete", /featuredPassDetailActionText[\s\S]*结束路线/],
  ["active city pass primary action uses the normal checkin flow before the last stop", /state\.activeRoute\?\.isFeaturedPass[\s\S]*status === "active"[\s\S]*!isRouteAtLastStop\(state\.activeRoute\)[\s\S]*openCheckinSheet/],
  ["active city pass no longer routes the primary button through continueFeaturedPass", { not: /else if \(status === "active"\) \{[\s\S]{0,260}continueFeaturedPass/ }],
  ["final city pass route can end while keeping unused benefits active", /completeFeaturedPassExploration[\s\S]*路线已走完，未核销权益仍可继续使用/],
  ["route-only city pass completion does not write profile records", { not: /function completeFeaturedPassExploration\(routeItem,\s*pass\)\s*\{[\s\S]{0,900}state\.records\.push/ }],
  ["final city pass primary action separates route completion from benefit completion", /isRouteAtLastStop\(routeItem\)[\s\S]*canCompleteFeaturedPass[\s\S]*completeFeaturedPass[\s\S]*completeFeaturedPassExploration/],
  ["expired city pass is archived as completed instead of staying expired", /completeExpiredFeaturedPass[\s\S]*expiredCompletedAt[\s\S]*status\s*=\s*"completed"/],
  ["completed ordinary routes are stored and removed from recommendations", /completedRouteIds|isRouteCompleted|routeRecommendationEligible[\s\S]*!isRouteCompleted\(routeItem\)/],
  ["ordinary recommendations avoid places already visited in the current city", /visitedStopSet[\s\S]*routeHasVisitedStop[\s\S]*routeRecommendationEligible[\s\S]*localRecommendedRoutes[\s\S]*new Set\(visitedStops\)[\s\S]*recommendationForLayer/],
  ["ordinary map list filters visited stops but featured city passes do not", /sortedRoutes[\s\S]*visitedStops\s*=\s*visitedStopSet\(\)[\s\S]*state\.layer === "featured"[\s\S]*featuredRoutesForCity\(state\.city\)[\s\S]*routeRecommendationEligible\(route,\s*visitedStops\)/],
  ["AI recommendation payload excludes visited places from candidates", /buildAiRecommendationPayload[\s\S]*visitedStops\s*=\s*visitedStopSet\(\)[\s\S]*poiByLayer[\s\S]*!visitedStops\.has\(normalizeStop\(title\)\)[\s\S]*不要推荐用户已经去过/],
  ["AI normalized routes avoid visited stops when expanding route stops", /normalizeAiRoute[\s\S]*visitedStops\s*=\s*visitedStopSet\(\)[\s\S]*normalizeStopList\(item\.stops\)\.filter[\s\S]*usedStops:\s*new Set\(visitedStops\)/],
  ["active featured pass uses compact three-button exploration actions", /data-pass-state="active"|route-detail-sheet\[data-mode="featured"\]\[data-pass-state="active"\]\s+\.route-actions[\s\S]*repeat\(3,\s*minmax\(0,\s*1fr\)\)/],
  ["orders and settings open real account panels", /accountSheet|renderOrdersPanel|renderSettingsPanel|订单中心|偏好设置/],
  ["profile order settings tools are compact", /\.profile-tools[\s\S]*display:\s*flex[\s\S]*min-height:\s*32px/],
  ["unpaid pass stop cards show benefits directly without expansion", /passStopCarouselHTML[\s\S]*cardTag[\s\S]*购买后可用/],
  ["old pass directory block was removed", { not: /站点目录|点击上方站点|点击展开店铺信息/ }],
  ["pre-purchase screen does not show purchase-to-redeem buttons", { not: /购买后核销/ }],
  ["there is no continue payment or saved pending state", { not: /继续支付|pending_payment|待支付订单|未支付/ }],
  ["featured pass styles exist", /\.featured-pass|\.pass-stop|\.profile-pass/],
  ["stylesheet cache key changed", /styles\.css\?v=20260624-pass-price/],
  ["script cache key changed", /script\.js\?v=20260624-pass-price/]
];

const combined = `${index}\n${script}\n${styles}\n${favicon}`;
const failures = checks
  .filter(([, pattern]) => pattern.not ? pattern.not.test(combined) : !pattern.test(combined))
  .map(([label]) => label);

if (failures.length) {
  console.error(`Missing featured pass prototype pieces:\n- ${failures.join("\n- ")}`);
  process.exit(1);
}

console.log("Featured pass prototype checks passed.");
